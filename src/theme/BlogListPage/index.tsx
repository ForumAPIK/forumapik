import React from 'react';
import BlogListPageContent from '@theme-original/BlogListPage';

export default function BlogListPageWrapper(props): React.ReactElement {
  return <BlogListPageContent {...props} />;
}
